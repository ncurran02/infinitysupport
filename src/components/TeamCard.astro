---
const {
    name,
    description,
    image
} = Astro.props;
---

<div class="card w-84 bg-white shadow-md text-[#116089] lg:mx-5 lg:my-0 my-5 hover:transform hover:scale-105 transition-transform duration-300">
    {image && (
        <figure class="avatar w-32 h-32 mx-auto mt-5">
            <img src={image} alt="Image" class="rounded-full" />
        </figure>
    )}
    <div class="card-body justify-center items-center text-center">
        <h2 class="card-title text-2xl">{name}</h2>
        <p class="text-black">{description}</p>
    </div>
</div>